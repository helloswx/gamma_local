# Gamma API 配置
# 请从 https://gamma.app 获取您的 API Key
# 将此文件重命名为 .env 并填入您的 API Key

GAMMA_API_KEY=your_gamma_api_key_here

# 可选：主题 ID（如果不设置，将使用工作区默认主题）
# 您可以通过 Gamma API 的 GET Themes 端点获取可用的主题列表
# 或者从 Gamma 应用中复制主题 ID
# GAMMA_THEME_ID=your_theme_id_here

# 可选：浏览器自动化设置
# 设置为 true 使用无头模式（不显示浏览器窗口），false 显示浏览器窗口（便于调试）
# BROWSER_HEADLESS=false

